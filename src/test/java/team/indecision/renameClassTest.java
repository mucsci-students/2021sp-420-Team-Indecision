package team.indecision;

import static org.junit.Assert.*;
import java.io.ByteArrayOutputStream;
import java.io.PrintStream;
import org.junit.Test;

public class renameClassTest {
/*
	@Test
	public void testRenameClass() {
		UML.addClass("test");
		UML.addClass("test1");
		UML.renameClass("test", "renameTest");
		assertFalse(UML.getClasses().containsKey("test")); //Test that the classes does not contain a key named test.
		assertTrue(UML.getClasses().containsKey("renameTest")); //Test that the class has been renamed.		
		
		ByteArrayOutputStream outContent = new ByteArrayOutputStream();
		System.setOut(new PrintStream(outContent)); //capturing console output.
		UML.renameClass("test", "123");
		assertEquals("The class test does not exist.", outContent.toString().trim()); //Test that the proper error message prints to the console when trying to rename a class that does not exist.
		System.setOut(System.out); // resetting the system.setOut to default
		
		ByteArrayOutputStream outContent1 = new ByteArrayOutputStream();
		System.setOut(new PrintStream(outContent1)); //capturing console output.
		UML.renameClass("renameTest", "test1");
		assertEquals("The new class name test1 already exists.", outContent1.toString().trim()); //Test that the proper error message prints to the console when trying to rename to a class that already exists.
		System.setOut(System.out); // resetting the system.setOut to default
	}
*/
}
